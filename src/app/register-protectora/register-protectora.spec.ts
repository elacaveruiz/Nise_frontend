import { RegisterProtectora } from './register-protectora';

describe('RegisterProtectora', () => {
  it('should create an instance', () => {
    expect(new RegisterProtectora()).toBeTruthy();
  });
});
