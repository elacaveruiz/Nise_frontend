import { loginDTO } from "./loginDTO";

export class RegisterProtectora {

    id: number;
    nombre: string;
    localidad: string;
    telefono: number;
    email: string;
    loginDTO: loginDTO[] = [];
}